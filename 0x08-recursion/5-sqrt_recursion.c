#include "main.h"

/**
 * _sqrt_recursion - returns the natural square root of a number
 * @n: the num to find the square root of
 * @r: the root to be tested
 * Return: the square root if n has a natural r, else -1
 */

int _sqrt_recursion(int n, int r)
{
	if ((r * r) == n)
		return (r);
}
